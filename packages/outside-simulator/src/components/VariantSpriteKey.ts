import { str } from 'bitecs/serialization';

/** Optional variant sprite key; renderer falls back to DefaultSpriteKey. */
const VariantSpriteKey = {
  value: str([]),
};
export default VariantSpriteKey;
