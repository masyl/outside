/**
 * Mass (kg) of the food item when launched as a projectile.
 * Lighter items travel faster; heavier items have more impact.
 */
const ProjectileMass = {
  value: [] as number[],
};
export default ProjectileMass;
