/**
 * Food canon: holds a reference to a loaded food entity and remaining ammo.
 * loadedFoodEid = 0 means empty (no food loaded).
 */
const FoodCanon = {
  loadedFoodEid: [] as number[],
  ammoRemaining: [] as number[],
};
export default FoodCanon;
