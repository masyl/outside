/**
 * Ammo capacity of a food item when used as a projectile.
 * max: total shots this food provides.
 * remaining: shots left before the food teleports back to the dungeon.
 */
const ShotCount = {
  max: [] as number[],
  remaining: [] as number[],
};
export default ShotCount;
