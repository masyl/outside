/** Default tile side length, in screen pixels. */
export const DEFAULT_TILE_SIZE = 16;

/** Native frame size of the character spritesheet. */
export const SPRITE_SIZE = 16;

/** Number of horizontal walk frames in one walk row. */
export const WALK_FRAMES = 4;

/** Number of completed walk cycles per traversed world tile. */
export const WALK_CYCLES_PER_TILE = 1.5;
