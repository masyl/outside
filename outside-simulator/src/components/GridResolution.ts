/**
 * Grid resolution: precision vs integer coordinates (e.g. 1 = integer cells, 0.125 = 8 snap positions per 1 unit).
 */
const GridResolution = {
  value: [] as number[],
};
export default GridResolution;
