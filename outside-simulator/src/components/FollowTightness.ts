/**
 * How quickly velocity is adjusted toward target (0 = instant, higher = slower).
 * Optional; used with Follow urge.
 */
const FollowTightness = {
  value: [] as number[],
};
export default FollowTightness;
